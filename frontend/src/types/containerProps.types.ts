// Props for a Container component that wraps child components inside a layout
export type ContainerProps = {
  /**
   * The child components to be rendered inside the container
   */
  children: React.ReactNode;
};
